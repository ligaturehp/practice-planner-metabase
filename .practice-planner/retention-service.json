{
  "startCommand": "psql -X -v ON_ERROR_STOP=1 -c \"SET statement_timeout = '30s'; DELETE FROM analytics.events WHERE occurred_at < now() - interval '90 days';\"",
  "cronSchedule": "17 7 * * *",
  "restartPolicyType": "NEVER",
  "numReplicas": 1
}
